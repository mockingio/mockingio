name: Example mock 2
routes:
  - request: GET /account
    description: "account detail"
    responses:
      - body: |
          {
            "name": "Test Account"
          }
  - request: POST /account
    responses:
      - body: |
          {
            "success": 1
          }